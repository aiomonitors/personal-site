{
  "name": "personal-site",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1",
    "nodejs-base64": "^1.0.3",
    "react-scripts": "3.4.1"
  },
  "engines": {
    "npm": "6.4.1",
    "node": "10.14.2"
  },
  "scripts": {
    "start": "node server/server.js",
    "build" : "cd server && rm -rf build && cd ../client && react-scripts build && mv build ../server"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
